---
description: Create an implementation plan from an approved spec
---
Create an implementation plan from an approved spec.

Input: $@

Requirements:
- Stay in the current session.
- Use the approved spec the user references, or infer the most recent approved spec when explicitly asked.
- Write the plan to `.pi/plans/YYYY-MM-DD-<topic>.md`.
- Break the work into strong, reviewable, commit-sized tasks.
- Include verification expectations and commit intent per task.
- Do not implement code.
- Do not commit generated docs.
